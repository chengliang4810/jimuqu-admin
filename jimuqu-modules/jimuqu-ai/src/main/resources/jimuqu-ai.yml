##ollama
#solon.ai.chat:
#  default:
#    apiUrl: "http://127.0.0.1:11434/api/chat" # 使用完整地址（而不是 api_base）
#    provider: "ollama" # 使用 ollama 服务时，需要配置 provider
#    model: "llama3.2" # 或 deepseek-r1:7b

# openai
solon.ai.chat:
  default:
    # 使用完整地址（而不是 api_base）
    apiUrl: "https://api-inference.modelscope.cn/v1/chat/completions"
    apiKey: "xxxxx"
    # provider: "openai" # 默认为openai
    model: "Qwen/Qwen3-8B"